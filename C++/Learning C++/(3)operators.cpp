/*Bitwise boolean operators (binary opeartions performed bit by bit between the operand )
 & = bitwise AND
 | = bitwise OR
 ~ = NOT
 ^ = XOR
it shoud be noted this are bitwise operation not logical
*/

/*
Logical operations are the ones you use with if statements
&& = AND
|| = OR
! = NOT
*/

/*
* = Gives you acces to the variable the pointer is pointing to (defrences the pointer to it's right)
& = "prefix &"" returns the address of the variable at it's right
-> = Gives you access to members of classes or structures from a pointer
*/